{
  "name": "shorts-cuter",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n BE,FE -c cyan,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "concurrently -n BE,FE -c cyan,magenta \"npm --prefix backend run build\" \"npm --prefix frontend run build\"",
    "start": "concurrently -n BE,FE -c cyan,magenta \"npm --prefix backend run start\" \"npm --prefix frontend run preview\"",
    "compose:up": "docker compose up -d",
    "compose:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
